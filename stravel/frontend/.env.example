# Backend URL for the Vite dev server proxy.
# Default (Ubuntu / Docker on same host):
# BACKEND_URL=http://localhost:8000
#
# macOS via Tailscale (copy to .env.local and set Ubuntu's Tailscale IP):
# BACKEND_URL=http://100.79.32.101:8000
